{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "command": "npm", // Could be any other shell command
            "args": ["run", "compile"],
            "type": "shell"
        }
    ]
}